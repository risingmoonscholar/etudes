[package]
name = "sweep-read"
version.workspace = true
edition.workspace = true
license.workspace = true

# Content inspection lives here, not in sweep-core, so the engine keeps zero
# dependencies and the no-network claim stays cheap to check.
[dependencies]
