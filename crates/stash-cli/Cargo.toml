[package]
name = "stash-cli"
version.workspace = true
edition.workspace = true
license.workspace = true

[[bin]]
name = "stash"
path = "src/main.rs"

[dependencies]
sweep-core = { path = "../sweep-core" }
sweep-keep = { path = "../sweep-keep" }

[dev-dependencies]
fixtures = { path = "../fixtures" }
