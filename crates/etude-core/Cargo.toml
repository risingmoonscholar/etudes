[package]
name = "etude-core"
version.workspace = true
edition.workspace = true
license.workspace = true

# Deliberately empty. The engine has zero dependencies, which makes the
# no-network claim trivially checkable: there is no third-party code in the
# classification path at all.
[dependencies]

[dev-dependencies]
fixtures = { path = "../fixtures", version = "0.5.0" }
